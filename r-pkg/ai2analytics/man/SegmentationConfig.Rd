% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segmentation.R
\name{SegmentationConfig}
\alias{SegmentationConfig}
\title{Build a segmentation configuration.}
\usage{
SegmentationConfig(
  analysis_name = "segmentation",
  col_entity_id = "entity_id",
  feature_columns = character(),
  exclude_columns = character(),
  n_segments = 4L,
  method = c("kmeans", "hierarchical", "auto"),
  normalize = TRUE,
  normalization_method = c("standard", "minmax", "robust"),
  handle_missing = c("median", "mean", "zero", "drop"),
  auto_select_k = FALSE,
  k_range = c(2L, 10L),
  output_csv = ""
)
}
\arguments{
\item{analysis_name}{Free-text label for the run.}

\item{col_entity_id}{Name of the column holding the entity identifier.}

\item{feature_columns}{Character vector of numeric columns to cluster on.
If empty, the pipeline auto-selects all numeric
columns except the entity ID.}

\item{exclude_columns}{Numeric columns to skip when auto-selecting.}

\item{n_segments}{Integer number of clusters when not auto-selecting k.}

\item{method}{One of \code{"kmeans"}, \code{"hierarchical"},
\code{"auto"}.}

\item{normalize}{Whether to normalise features before clustering.}

\item{normalization_method}{One of \code{"standard"} (z-score), \code{"minmax"},
\code{"robust"} (median/IQR).}

\item{handle_missing}{One of \code{"median"}, \code{"mean"}, \code{"zero"},
\code{"drop"}.}

\item{auto_select_k}{If \code{TRUE}, search over \code{k_range}.}

\item{k_range}{Integer length-2 vector \code{c(low, high)}.}

\item{output_csv}{Optional path to write the assignments CSV.}
}
\value{
A list with class \code{"SegmentationConfig"}.
}
\description{
Externalises every schema decision so the same pipeline class works across
regions and brands.
}
\examples{
cfg <- SegmentationConfig(
  analysis_name   = "demo",
  col_entity_id   = "npi",
  feature_columns = c("IL_17_TRX_L12M", "IL_23_TRX_L12M"),
  n_segments      = 4
)
}
